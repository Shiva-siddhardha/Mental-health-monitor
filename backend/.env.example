# Backend Environment Variables
# Copy this file to .env and fill in your actual values
# NEVER commit the .env file to version control

# Server Configuration
PORT=5000
# The port on which the backend server will run (default: 5000)

# Database Configuration
MONGO_URI=your_mongodb_connection_string
# MongoDB connection string from MongoDB Atlas
# Example: mongodb+srv://username:password@cluster.mongodb.net/dbname?retryWrites=true&w=majority
# Get this from: https://cloud.mongodb.com/ -> Connect -> Connect your application

# Session Configuration
SESSION_SECRET=your_session_secret_here
# Secret key for Express session encryption
# Generate a random string (recommended: 32+ characters)
# Example: Use `openssl rand -base64 32` to generate a secure secret

# JWT Configuration
JWT_SECRET=your_jwt_secret_here
# Secret key for JSON Web Token signing
# Generate a random string (recommended: 32+ characters)
# Should be different from SESSION_SECRET
# Example: Use `openssl rand -base64 32` to generate a secure secret

# CORS Configuration
FRONTEND_URL=http://localhost:3000
# URL of the frontend application for CORS configuration
# Development: http://localhost:3000
# Production: Set this to your deployed Vercel URL (e.g., https://your-app.vercel.app)

# Optional: additional allowed origins (comma-separated). Useful for Vercel preview deployments or multiple frontends.
# Example: ALLOWED_ORIGINS=https://preview-deploy-xyz.vercel.app,https://another-app.example.com
ALLOWED_ORIGINS=

# AI/ML Configuration
GEMINI_API_KEY=your_gemini_api_key_here
# Google Gemini API key for AI-powered features
# Get this from: https://makersuite.google.com/app/apikey
# Used for AI therapist and quiz features

# Note: The sentiment analysis feature automatically falls back to local processing.
# No external ML service is required.
